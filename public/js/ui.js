export function escapeText(value){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
export function updateStatus(state){const s=state.status||{};document.getElementById('statusText').textContent=(s.state||'offline').toUpperCase();document.getElementById('statusDot').classList.toggle('online',s.online);document.getElementById('botName').textContent=s.username||'—';document.getElementById('botState').textContent=s.state||'offline';document.getElementById('health').textContent=`${s.health||0} / 20`;document.getElementById('food').textContent=`${s.food||0} / 20`;const p=s.position||{x:0,y:0,z:0};document.getElementById('position').textContent=`X:${p.x} Y:${p.y} Z:${p.z}`;document.getElementById('angles').textContent=`${s.yaw||0} / ${s.pitch||0}`;document.getElementById('dimension').textContent=s.dimension||'—';document.getElementById('gameMode').textContent=s.gameMode||'—';document.getElementById('ping').textContent=s.ping??'—';}
export function addChat(line){const el=document.createElement('div');el.className=`chat-line ${line.kind==='sent'?'sent':line.kind==='system'?'system':''}`;el.innerHTML=`<strong>${escapeText((line.kind||'server').toUpperCase())}</strong> ${escapeText(line.message)}`;const history=document.getElementById('chatHistory');history.appendChild(el);history.scrollTop=history.scrollHeight;}
export function showError(message){addChat({kind:'system',message});}
export function setupCollapsibles(){document.querySelectorAll('.collapse').forEach(btn=>btn.addEventListener('click',()=>document.getElementById(btn.dataset.target).classList.toggle('collapsed')));}
